// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { JournalTrade } from '../mega-flowz/journal-trade.js';


export class JournalData {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):JournalData {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsJournalData(bb:flatbuffers.ByteBuffer, obj?:JournalData):JournalData {
  return (obj || new JournalData()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsJournalData(bb:flatbuffers.ByteBuffer, obj?:JournalData):JournalData {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new JournalData()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

trades(index: number, obj?:JournalTrade):JournalTrade|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new JournalTrade()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

tradesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startJournalData(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addTrades(builder:flatbuffers.Builder, tradesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, tradesOffset, 0);
}

static createTradesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTradesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endJournalData(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createJournalData(builder:flatbuffers.Builder, tradesOffset:flatbuffers.Offset):flatbuffers.Offset {
  JournalData.startJournalData(builder);
  JournalData.addTrades(builder, tradesOffset);
  return JournalData.endJournalData(builder);
}
}
